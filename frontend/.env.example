# Add environment files to .gitignore to avoid committing sensitive information
.env.local
.env.production.local

# Keep .env and .env.production for template purposes
# Make sure to update .env.production with your actual production API URL
